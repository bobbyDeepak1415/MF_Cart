

Micro frontend architecture

simple react-vite e-commerce application.consists a custom hook fecthing data.the data displayed in return statement.


App.js has two child components. ProductsListing.jsx and ProductCart.jsx. These two are the two features(MFES).
